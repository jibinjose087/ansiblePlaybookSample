pipeline {
    agent any
        environment {
          VERSION = NEW
        }
            stages {
                stage ('deployment stage'){
                    steps {
                    echo "Version number is $VERSION"
                    }
        }
    }
}
